# stack-aegis

Python resource budgets for the Exemplar stack. Wrap blocking calls with declared timeouts, optional fallbacks, and structured tags.

Install from this directory with `pip install .`, or install development dependencies with `pip install ".[dev]"`.

See the [project documentation](https://github.com/wandercom/aegis) for API examples, the shared TypeScript/Python contract, and golden-vector verification.
